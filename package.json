{
  "name": "dynamic-graph-creator",
  "version": "1.0.0",
  "description": "Dynamic graph creation library with ECharts",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "sass": "^1.69.0"
  },
  "dependencies": {
    "autosize": "^6.0.1",
    "sortablejs": "^1.15.6"
  }
}
